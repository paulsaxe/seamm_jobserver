name: test

channels:
  - conda-forge
  
dependencies:
    # Base depends
  - python
  - pip

  # molsystem requires, and only available via Conda.
  - openbabel

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

  - py-cpuinfo

  # Pip-only installs
  - pip:
    - seamm

    # Testing
    - yapf

