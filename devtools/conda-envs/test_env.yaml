name: test

channels:
  - conda-forge
  
dependencies:
    # Base depends
  - python
  - pip

  # Dependencies for this module
  - seamm

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  - py-cpuinfo

  # Pip-only installs
  - pip:
    # Testing
    - yapf

    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer

